using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.UI;

public class Timer : MonoBehaviour
{
	[SerializeField] int timeLimit;			// ?c????????????????????
	[SerializeField] Text timerText;		// ?c?????????\??????????
	float time;								// ?c?????????v?Z????????

	void Update()
	{
		time += Time.deltaTime;					// ?t???[???????o????????time??????????
		int remaining = timeLimit - (int)time;	// time??????int?^??????????????????????????????limit??????????
		if ( remaining > 0 )
		{
			timerText.text = $"{remaining.ToString("D3")}";		// timerText???X?V????????
		}
		else													// ?c??????0????????????????
		{
			timerText.text = $"{0}";                            // 0???\??
			SceneManager.LoadScene("ResultScene");
		}
	}
}